import Foundation

enum AppConfig {
    /// Base URL of the Cloudflare-hosted static Confluence bundle (live).
    static let confluenceBaseURL = URL(string: "https://adfreader-confluence.pages.dev")!
}
